// PLACEHOLDER — replace this file with your team's real match-data.js
// (the one already referenced in your original site / GitHub repo).
// It should define window.MATCH_DATA as an object keyed by event key,
// e.g. window.MATCH_DATA = { "2026txsan": { 1: ["frc118", ...], ... } };
window.MATCH_DATA = {};
